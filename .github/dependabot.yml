# Keep the SHA-pinned actions and the hash-pinned release tools current.
# Dependabot opens a pull request; nothing changes without review.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: pip
    directory: /requirements
    schedule:
      interval: weekly
